#!/usr/bin/ruby
# -*- encoding : utf-8 -*-

LinaelLanguages = 'fr'
require_relative '../config/environment'

module Linael

  #The server to join
  ServerAddress = 'irc.rizon.net'
  #Nick of the bot
  BotNick = 'LinaelTest'
  Master = 'zaratan'

  Core::start_linael do
    Core::start_server :irc, master_module: MasterModule, modules: ModulesToLoad, name: "rizon", url: ServerAddress, nick: BotNick, port: Port
    Core::start_server :irc, master_module: MasterModule, modules: [], name: "iiens", url: 'irc.iiens.net', nick: BotNick, port: Port
  end


end
